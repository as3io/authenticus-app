<div class="sticky-top z-depth-1-half">
  <nav class="navbar navbar-expand navbar-dark bg-dark">
    {{#link-to "authenticated" class="navbar-brand"}}<span class="text-primary">Auth</span>enticus{{/link-to}}

    <ul class="navbar-nav mr-auto">
      {{!-- {{#if user.isAuthenticated}}
        {{#link-to "logout" tagName="li" class="nav-item"}}
          {{#link-to "logout" class="nav-link" title="Log Out"}}
            {{entypo-icon "log-out"}} <span class="d-none d-md-inline">Log Out</span>
          {{/link-to}}
        {{/link-to}}
      {{/if}} --}}
    </ul>
    <ul class="navbar-nav float-lg-right">
      {{user-nav model=user.model}}
    </ul>
  </nav>
  {{loading-progress class="bg-dark"}}
</div>
